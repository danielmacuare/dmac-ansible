---
- name: "INSTALLING - APT Packages"
  become: true
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  with_items: "{{ apt_packages }}"
  loop_control:
    label: "Install APT Package: {{ item }}"
